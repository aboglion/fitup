# משימות לתיקון - FitUp v15.6

לאחר בדיקת כל הטענות לעומק מול הקוד (מנוע ה-Progression וממשק המשתמש), להלן הנקודות שנמצאו נכונות ומהוות שיפור אמיתי שצריך ליישם. (הערה: שאר הנקודות נמחקו כיוון שהן כבר פתורות בארכיטקטורה הקיימת - ה-UI של Myo-Reps כבר משתמש ב-3 כפתורים פשוטים ולא דורש ספירה ידנית, הסנכרון כבר מטופל ע"י בחירת גרסה פשוטה ובטוחה, וכפתורי ה-UI כבר מתורגמים לשפה טבעית ולא מציגים ז'רגון טכני).

- [x] **מלכודת ה-Softened Progression וההיסטוריה החסרה (Session Gap Time Decay):**
  - **הבעיה בקוד:** הפונקציה `checkSoftenedProgression` ב-`progression.js` מסתמכת על `previousSessionData`, אך אינה בודקת כמה זמן עבר מאז אותו אימון. אם מתאמן פספס שבועיים או היה חולה, הוא עלול לקבל העלאת משקל (Softened increase) למרות היחלשות (Detraining), מה שעלול לגרום לפציעה.
  - **הפתרון (הטוב והנכון ביותר):** הוספת תנאי ב-`progression.js`. אם הפער בין האימון הנוכחי ל-`previousSessionData` גדול מ-10 ימים, ה-`softened progression` יבוטל אוטומטית וידרוש `all_above` רגיל להעלאת משקל.

- [x] **דיווח אינטראקטיבי על כאב מפרקים (Joint Pain Reporting) עבור ה-Arm Block:**
  - **הבעיה בקוד:** מנוע ההתקדמות ב-`progression.js` מתייחס ל-`clusterResults.jointPainReported` כדי לבטל את בלוק הזרועות (`cancel_block`), אך בממשק (UI) ב-`today.js` אין כרגע שום מקום שבו המשתמש יכול לדווח על הכאב הזה כשהוא בוחר ב-BELOW.
  - **הפתרון (שדרוג UX קריטי):** כאשר המשתמש בוחר בכפתור ה-⚠️ (BELOW) עבור תרגילי Arm Block (כגון Single-Arm Curl או Overhead Triceps), יש להקפיץ מיידית חלונית אישור (Pop-up): "האם הסיבה לכישלון היא כאב במרפק/כתף?". אם הוא בוחר ב"כן", המערכת תרשום `jointPainReported: true` ותעביר זאת למנוע לצורך ביטול הבלוק.